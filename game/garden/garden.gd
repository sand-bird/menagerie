extends Node2D

# class member variables go here, for example:
# var a = 2
# var b = "textvar"

func _ready():
#	var m = get_node("monster")
	
#	var j = m.serialize().to_json()
#	var data = {}
#	data.parse_json(j)
#	m.deserialize(data)
	
	# m.serialize()
#	var test = Utils.weighted_average([[100, 1], [1, 3]])
#	print("test weighted average: ", test)
#	print("iq: ", m.iq.value, " | size: ", m.size)
	
#	print("testing rand_tri for range 0 to 100 with mean 80...")
#	var results = {}
	
#	for i in range(1, 10000):
#		var x = Utils.weighted_avg([[Utils.rand_tri(60, 90, 80), 1], [80, 3]])
#		var x = Utils.rand_parab(0, 100, 80)
#		if results.has(x): results[x] += 1
#		else: results[x] = 1
	
#	var keys = results.keys()
#	keys.sort()
#	for k in keys:
#		print(k, "\t", results[k])
	
#	var Monster = preload("res://monster/monster.gd")
#	var m_dad = Monster.new(null, null)
#	m_dad.name = "Dad"
#	var m_mom = Monster.new(null, null)
#	m_mom.name = "Mom"
	
#	var m = Monster.new(m_dad, m_mom)
	pass
