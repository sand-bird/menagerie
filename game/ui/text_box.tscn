[gd_scene load_steps=5 format=2]

[ext_resource path="res://assets/ui/theme.tres" type="Theme" id=1]
[ext_resource path="res://assets/ui/panels/panel_frame.tres" type="StyleBox" id=2]
[ext_resource path="res://ui/background_panel.tscn" type="PackedScene" id=3]
[ext_resource path="res://ui/text.gd" type="Script" id=4]

[node name="Panel" type="Panel"]
margin_right = 312.0
margin_bottom = 52.0
theme = ExtResource( 1 )
custom_styles/panel = ExtResource( 2 )

[node name="background" parent="." instance=ExtResource( 3 )]

[node name="text" type="RichTextLabel" parent="."]
visible = false
anchor_right = 1.0
anchor_bottom = 1.0
margin_left = 10.0
margin_top = 8.0
margin_right = -10.0
margin_bottom = -8.0
theme = ExtResource( 1 )
text = "Hello! This is a store. We should have room for two or maybe three lines of text so we can be sure to fit an item description.

Soft, springy fur from a Pufig's fluffy tail. It's rumored that stuffing a pillow with it will bring good dreams."
scroll_active = false

[node name="Control" type="Control" parent="."]
margin_right = 40.0
margin_bottom = 40.0
theme = ExtResource( 1 )
script = ExtResource( 4 )

